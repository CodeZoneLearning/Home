# Scripts e notebooks

Esta aula compara `.py` e `.ipynb` pelo modelo de execução, versionamento, testes, narrativa e ciclo de vida.

```bash
python examples/equipment_summary.py
```

O notebook importa a mesma função do script. Execute-o a partir da pasta `examples/`.
